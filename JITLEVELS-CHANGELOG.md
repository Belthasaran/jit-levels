- **JIT.Trans Invictus-style OW pointers (phase 2)**: Detect bank-before-LDX LevelNumberMap stubs and `$00B8DE` `LDA $8A / EOR #imm16` trampolines (Invictus `$158E` → `$DDB538`); decompress LZ2 then LZ3 in pure JS; never vanilla-scan when the LM OW stub is present; prefer `levelnumbermap` coords over stale DB/tilemap on merge. Invictus 006 → submap 0 tile 6,1 (no tile 87). Tests: `./enode.sh tests/test_jit_trans.js`.
- **JIT.Trans LM LevelNumberMap (no silent vanilla fallback)**: Detect Lunar Magic overworld hijack in pure JS (`$04D807`/`$04D803`/`$04D808`) without spawning asar; decompress LevelNumberMap for all LM hacks. When the hijack is present, never fall back to the vanilla `$0CF7DF` tilemap scan (that invented wrong coords, e.g. Invictus THE BRIDGE 24,3 vs real `$1F1F/$1F21` = 6,2). Decode failure → warning + blank coords. Export `trans_source` / `location_count`; primary tile = min (submap, y, x). Detected Levels shows source tooltips. Tests: `./enode.sh tests/test_jit_trans.js` (optional `JIT_TRANS_FIXTURE_ROM`).
- **JIT.Score scoring speed**: Pre-parse fingerprints to `Uint16Array` once, early-exit Hamming, and one-pass internal similarity so Detect Levels “Computing scores…” finishes in well under 1s for ~95 SMW levels (was ~38s). Tests: `./enode.sh tests/test_level_fingerprint.js`.
- **JIT.Score Orig density + diversity weighting**: Originality/internal similarity ignore screens with <5% nonzero tiles, then use an interest-weighted average (`density × invDom × uniqueNorm`) so sparse/mono pads cannot tank Orig (acidtapes `103` was Orig 3 from a 3%-dense tail). Completeness unchanged as the emptiness metric. Tests: `./enode.sh tests/test_level_fingerprint.js`.
- **JIT.Score Map16 expand in JS**: Ported `obj_to_map16` + `lm_level_expand` into `lib/jit-levels/levelinfo/` so fingerprints need no native binary on AppImage/portable EXE. Fixed LM 0x22/0x23 decode (map16 from raw[3]/B bit). Parity vs `lm_pipeline_dump --fingerprint`: SMW `105`, ako `109`, acidtapes `133`/`138`. Tests: `./enode.sh tests/test_jit_map16_expand_parity.js`.
- **JIT.Score fingerprint v2 + empty-level filtering**: Originality/completeness now treat empty levels as `0` (not fake `100`); missing corpus yields unscored `null`. Fingerprints use expanded Layer1 Map16 (`v2:` 16-bit tiles, 16×27 screens). CLI always emits `empty` rows. Vanilla SMW corpus populated in `electron/data/level_fingerprints.txt`. Detected Levels adds LowCompleteness (`<10`) and LowOriginality (`<15`) exclude filters. Fixed `--out=` CLI parse. Tests: `./enode.sh tests/test_level_fingerprint.js`.
- **Detected Levels "Show Existing" diff + merge**: The Detected Levels dialog (`DetectedLevelsDialog.vue`) adds a `Show Existing:` control next to the name-reader toggle with three modes: `If Different` (default), `Hide`, `Show All`. In `If Different`, stages already in `gamestages` (matched by `gameid`+`levelnumber`, version-aware) are surfaced when JITLevels detection disagrees, marked with `**` and a brighter row. Differing X/Y/Submap cells get a red border with a `Detected <d> vs existing <e>` tooltip; a `Δ` badge enumerates non-coordinate diffs (name, flags, tags, notes). Diff rules (one-directional): coordinate change/fill, detected level-type flag (`water/ghouse/spalace/castle/boss`) missing on the stage, detected tags/notes missing, or a blank/`-` existing name when JITNames2 found a meaningful (non-vanilla) name — admin-added extras are never treated as diffs. `Add Selected` on an existing stage now MERGES into it (no duplicate): coordinates overwritten from detection, flags OR-merged, tags union-merged, notes appended, name filled only when blank; manual fields (`requisites`, `playable`, `rando`, `secret/troll/final/lock`, `playlevel_patch_code`, `versions`, `stage_uuid`) preserved. Logic lives in `electron/renderer/src/utils/detected-stage-merge.js`. Tests: `test:detected-stage-merge`. See `devdocs/DETECTED_LEVELS_SHOW_EXISTING.md`.
- **JIT.Trans overworld coordinate detection fixes**: Verified against Lunar-Magic fixtures in `lmlevelinfo/test/*` (Callisto `.mwl` ground truth). Fixed `lib/jit-levels/jit-trans.js` `readLevelNumberMap` reading the wrong decompressor field (`.data` -> `.bytes`), which had made the compressed LM LevelNumberMap never decode and silently fall back to the vanilla sequential tilemap scan (wrong positions, never submap >1). Extended the submap scan from 2 to all 7 submaps (`LEVELNUMBERMAP_MAX`, bounded by decompressed length) in both the JS parser and `find_translevels.py`, and resolved the assembler via `binary-finder.findAsar()` instead of bare `asar` (another silent fallback). Verified QuickieWorld's `0x101-0x110` cluster lands on submap 1 (e.g. `0x101` at tile 23,11). Added synthetic all-submap decode tests plus a fixture-guarded regression test in `tests/test_jit_trans.js`.
- **MT compatibility with Share codes in Prepare Run**: Share → Load Share Code parses ik1–ik6 codes, resolves games via patchblobs/catalog/JIT, supports `raw_code` plan entries (migration 076), and maps switch-palace (`pall`) / free-play flags.
- **JIT MT-compat level detection**: Added `lib/jit-levels/mtcompat-levelreader.js` plus `jit-names2`, `jit-mt`, `jit-ow` extractors; Detected Levels UI gets JITNames/JITNames2 toggle, new source filters, exclude presets, and MT-only preset; tests `test_jit_names2`, `test_jit_mt`, `test_jit_ow`.
- **Detected Levels defaults**: Adding stages from JIT Detected Levels checks P/R for untested levels and sets Difficulty from gameversion numeric mapping (fallback 2).
- **JIT.LevelInfo**: Added `lib/jit-levels/levelinfo/` JS port of `lmlevelinfo` parse pipeline (`parseLevelInfo`, `mapLevelInfoToStageDefaults`, `deriveTagsFromLevelInfo`); parity with `level_info1 --json` on akogare `0x109`.
- **JITLevels1**: Just-in-time level detection in Game Stages dialog (JITNames, JIT.Trans, JIT.LMFilter, JIT.LevelInfo, JIT.Score); `lib/jit-levels/` modules; `jstools/level_fingerprint.js` CLI; catalog LMFilter lookup by patched ROM SHA1; Map16 screen fingerprints for scoring. Fix: level ids like `130` now parse as hex 0x130 for LevelInfo/stage defaults (water, tags, description). LMFilter Calisto prompt uses Yes/No confirm dialog.
